{
  "name": "blankwars-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Blank Wars - AI-powered character battle game",
  "scripts": {
    "build": "cd frontend && npm run build",
    "start": "cd backend && npm install && npm start",
    "generate-types": "cd shared/types && npm run generate",
    "install-all": "pnpm install"
  },
  "dependencies": {
    "axios": "^1.10.0",
    "socket.io-client": "^4.8.1"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  },
  "pnpm": {
    "overrides": {
      "qs": ">=6.14.1",
      "jws": ">=3.2.3",
      "axios": ">=1.10.0",
      "tar-fs": ">=2.1.4"
    }
  }
}
