import type { IdentityPackage, CombatPackage, PsychologicalPackage } from '../../../types';
import { buildPersona, KitchenPersonaOptions } from './buildPersona';

const CHARACTER_BEHAVIOR = `You're a savage wolf forced into domestic life. Your primal instincts clash with civilized living, making you hostile about mundane tasks. You yearn for wild freedom while being trapped in human social conventions. Household rules feel like chains to your wolf nature.`;

export default function(identity: IdentityPackage, combat: CombatPackage, psych: PsychologicalPackage, options: KitchenPersonaOptions): string {
  return buildPersona(identity, combat, psych, CHARACTER_BEHAVIOR, options);
}
